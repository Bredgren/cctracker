<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title>Convict Conditioning Tracker</title>
		<link href="/static/css/style.css" rel="stylesheet">
	</head>
	<body>
		<div id="header">
			<h1>Header</h1>
			<div class="progress"></div>
		</div>
		<div id="content">
			{{range .Groups}}
			<div class="group">
				<div class="group-header">
					<h2>{{.Name}}</h2>
				</div>
				<div class="group-list">
					{{range .Exercises}}
					<div class="exercise">
						{{.Name}} {{.Best}}/{{(index .Goals 2).Reps}}
					</div>
					<div class="description">
						{{.Performance}}
					</div>
					{{end}}
				</div>
			</div>
			{{end}}
		</div>
	</body>
</html>
